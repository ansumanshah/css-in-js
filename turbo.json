{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "format": {},
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "vscode:publish": {
      "dependsOn": ["^build"]
    }
  }
}
